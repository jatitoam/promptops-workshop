// Pendiente — tarea 2.4 del CHECKLIST del taller.
// conexion LLM de Groq + evaluador + regla, por API — via de escape del bloque 6
console.error(
  "Aún no implementado: conexion LLM de Groq + evaluador + regla, por API — via de escape del bloque 6.\n" +
    "Corresponde a la tarea 2.4 del CHECKLIST de construcción del taller.",
);
process.exit(1);
